import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const SignUpPage = () => {
  return (
    <SafeAreaView>
      <Text>Ini sign up</Text>
    </SafeAreaView>
  );
};

export default SignUpPage;
